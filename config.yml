title: git-curso
description: mi  primer proyecto con Git
template:
  repo: git-curso-template
  name: git-curso
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
